#include <stdio.h>


/**
* main - prints the alphabet in lowercase, and then in uppercase,
* followed by a new line
* Return: Always 0 (Success)
*/

int main(void)
{
	int alph;

	for (alph = 'a'; ch <= 'z'; alph++)
		putchar(alph);
	for (ch = 'A'; ch <= 'Z'; ch++)
		putchar(ch);
		putchar('\n');
	return (0);

}
